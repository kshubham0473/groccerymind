# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
build/
next-env.d.ts

# env
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# os
.DS_Store
*.pem

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── GroceryMind corpus pipeline ───────────────────────────────────────────────
# Embedding resume cache — regenerable, large, and noisy in diffs.
# NOTE: this file is currently TRACKED. Untrack it once with:
#   git rm --cached lib/.embed-cache.json
lib/.embed-cache.json

# Backups written by scripts/backfill-dish-images.js
lib/*.json.bak

# Raw enrichment output. Regenerable via scrape → enrich → split; only the
# split runtime files (dishes-meta.json, dishes-embeddings.json) are needed at
# runtime and must stay committed for outputFileTracingIncludes.
# Uncomment if the repo gets too heavy:
# lib/dishes-corpus-v2.json
